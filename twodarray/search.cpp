#include <stdio.h>
#include "array2d.h"

int main()
{
  int n = 10;
  Array arr(n);

  /* insert code to find value here
     
     1. Get value input from stdin
     2. Look for value in arr in log(n) time
    
     N.B. You can access data elements of arr via arr[i][j], where i, j < n
  */

  return 0;
}
